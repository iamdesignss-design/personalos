# Personal OS

A private, offline-friendly personal operating system prototype.

## GitHub Pages

This app can be published as a static site. GitHub Pages should serve the root folder from the `main` branch. `index.html` redirects to the self-contained `bundle.html`.
